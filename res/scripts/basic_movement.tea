import pomegranate

function Update(delta)
{
    if(input_down('Z'))
    {
        //Jump or something
    }
}
function Draw()
{
    debug_sphere(entity_get_position(entity_get_id()),1.0) //Position, Radius
}