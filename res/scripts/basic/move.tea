import Entity
import Pomegranate

var time = 0.0

var what = 1

var movement = [0.0,0.01,0.0]

function editor_update()
{
    time += 0.0166
    Entity.set(what,"position", Pomegranate.add3(Entity.get(what,"position"),movement))
}